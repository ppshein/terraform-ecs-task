# Backend configuration for CORE STAGING environment
region         = "eu-west-1"
bucket         = "terraform-state-digital-core-staging"
key            = "core/sre/staging/terraform.tfstate"
dynamodb_table = "terraform-state-lock-digital-core-staging"
encrypt        = true

# Optional: Use assume role for cross-account access
# role_arn = "arn:aws:iam::TARGET-ACCOUNT:role/TerraformRole"